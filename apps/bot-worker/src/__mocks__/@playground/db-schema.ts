// Mock for @playground/db/schema used in tests
// These are just placeholder symbols — the actual queries use mocked tx objects

export const sessions = Symbol('sessions')
export const sessionScores = Symbol('sessionScores')
export const tracks = Symbol('tracks')
export const playlists = Symbol('playlists')
export const tenants = Symbol('tenants')
